# Copy this file to .env.local (recommended) or .env in this folder, then restart: npm run dev
#
# Supabase — Project Settings → API → Project URL + anon public key
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenRouter — same key as backend/src/main/resources/application.properties (careerpilot.openai.api-key)
VITE_OPENROUTER_API_KEY=your_openrouter_api_key
# Chatbot / HR / resume — quality model
VITE_OPENROUTER_MODEL=gpt-oss-120b
# Quizzes & coding generation — use a FAST model (much quicker loads)
# Examples: google/gemini-2.0-flash-001, meta-llama/llama-3.1-8b-instruct:free
VITE_OPENROUTER_FAST_MODEL=google/gemini-2.0-flash-001
